name: Profile checker

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Curl GitHub Profile
        run: curl -s https://github.com/Nkwenti-Severian-Ndongtsop 